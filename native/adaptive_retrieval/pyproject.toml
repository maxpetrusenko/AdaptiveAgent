[build-system]
requires = ["maturin>=1.14,<2"]
build-backend = "maturin"

[project]
name = "adaptive-retrieval"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
bindings = "pyo3"
module-name = "adaptive_retrieval"
